fastapi==0.115.6
uvicorn[standard]==0.34.0
# 双方向ストリーミング用。Developer Preview のためバージョンは固定しない。
# awscrt エクストラが必須（標準の HTTP トランスポートは HTTP/2 の
# 双方向イベントストリームに対応していない）。
aws-sdk-bedrock-runtime[awscrt]

# 会話履歴の DynamoDB 読み書き用
boto3==1.35.99
aws-credentials-http
